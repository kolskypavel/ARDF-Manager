plugins {
    id("com.android.library")
    id("org.jetbrains.kotlin.multiplatform")
}

kotlin {
    androidTarget()
    jvm("desktop")

    sourceSets {
        commonTest {
            dependencies {
                implementation kotlin("test")
            }
        }
    }
}

android {
    namespace = "org.openardf.radioomanager.shared"
    compileSdk = 36

    defaultConfig {
        minSdk = 26
    }
}
